<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="imagenBotonPulsado"> <!-- No lo uso, uso el style de abajo para las 3 imágenes del fragment menu -->
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <style name="imagenesPulsablesMenu"> <!-- Uso este para las 3 imágenes del fragment menu -->
        <item name="android:backgroundTint">@color/bgImg</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:contentDescription">@string/ib_info</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

</resources>
